# GitHub personal access token (classic or fine-grained)
# Required for real AI analysis via GitHub Models API (gpt-4o-mini).
# Without this, the app falls back to heuristic analysis.
# Grant: no extra scopes needed for GitHub Models access.
GITHUB_TOKEN=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
# Absolute path for the persistent JSON data file.
# On Azure App Service, point this to a mounted storage volume path.
# Example: /home/data/app-data.json
# DATA_FILE=/home/data/app-data.json

# HTTP port (default: 3000)
# PORT=3000
