# EasyStart PRD (Product Requirements Document)

## 1. 문서 목적
EasyStart의 제품 목표, 대상 사용자, 핵심 기능, 성공 지표, 범위/비범위를 정의해
제품 개발·검증·운영 시 공통 기준으로 사용한다.

## 2. 제품 개요
- **제품명**: EasyStart
- **한 줄 설명**: 피로도 기반으로 오늘 할 일을 추천하고, AI 분석/수정 학습 루프를 제공하는 개인 생산성 웹앱
- **핵심 가치**
  - 지금 상태(피로도)에 맞는 할 일 우선순위 제시
  - 작업 난이도/예상시간/집중도 분석으로 시작 장벽 감소
  - 사용자 수정을 반영해 개인화 정확도 개선

## 3. 문제 정의
사용자는 할 일 목록이 있어도 다음 작업을 고르기 어렵고, 현재 컨디션을 반영한 실행 계획을 세우기 어렵다.
또한 AI 추정이 부정확할 때 수동 교정과 재학습이 쉽지 않다.

## 4. 목표 및 비목표
### 4.1 목표 (Goals)
1. 사용자가 1분 내 “지금 할 일”을 결정할 수 있도록 한다.
2. 작업 생성 시 자동 분석 결과를 제공하고, 실패 시에도 서비스 이용을 지속 가능하게 한다.
3. 수동 수정 결과를 학습 프로필에 반영해 이후 분석 품질을 점진 개선한다.
4. 주간 완료율/피로도/미루는 작업 난이도를 확인할 수 있게 한다.

### 4.2 비목표 (Non-goals)
- 팀 협업(공유 보드, 멀티 유저 권한, 댓글)
- 캘린더 양방향 동기화
- 모바일 네이티브 앱 제공
- 고급 장기 통계(월/분기 리포트, 코호트 분석)

## 5. 대상 사용자
1. **개인 생산성 사용자(주 사용자)**: 해야 할 일은 많지만 우선순위 판단이 어려운 사용자
2. **비기술 사용자**: AI/인프라 오류 시에도 기능이 깨지지 않길 원하는 사용자

## 6. 핵심 사용자 시나리오
1. 사용자가 작업을 추가한다.
2. 시스템이 난이도/예상시간/집중도를 자동 분석한다.
3. 사용자가 홈에서 피로도를 입력한다.
4. 시스템이 피로도+긴급도+적합도를 반영해 추천 순서를 제시한다.
5. 사용자가 분석값을 수정하면 학습 프로필이 업데이트된다.
6. 사용자가 작업 완료 후 주간 기록 화면에서 요약을 확인한다.

## 7. 기능 요구사항 (Functional Requirements)
### FR-01 작업 생성
- 제목은 필수 입력값이어야 한다.
- 작업 생성 시 `id`, 생성/수정 시각, 완료 상태를 저장해야 한다.
- 생성 즉시 분석을 수행하고 결과를 작업에 저장해야 한다.

### FR-02 AI/휴리스틱 분석
- 분석 결과는 최소 다음 필드를 포함해야 한다:
  - difficulty (easy|medium|hard)
  - estimatedMinutes (10~180)
  - focusLevel (low|medium|high)
  - focusRequired (boolean)
  - todaySuitabilityScore (1~100)
  - reason, source
- Copilot SDK 또는 GitHub Models 호출 실패 시 휴리스틱 fallback을 사용해야 한다.

### FR-03 피로도 입력
- 사용자는 1~10 피로도를 입력할 수 있어야 하며, 범위를 벗어나면 서버에서 보정(clamp)해야 한다.

### FR-04 추천 순위
- 미완료 작업만 추천 대상이어야 한다.
- 피로도/난이도/긴급도/적합도 기반 점수로 정렬해야 한다.
- 추천 사유를 사용자에게 텍스트로 제공해야 한다.

### FR-05 분석 결과 수동 수정 + 학습
- 사용자는 난이도/예상시간/집중도/적합도/사유를 수정 가능해야 한다.
- 유효하지 않은 난이도/집중도는 저장을 거부해야 한다.
- 수정 시 학습 프로필(durationMultiplier, difficultyBias, focusBias, editCount)을 갱신해야 한다.

### FR-06 작업 완료 및 삭제
- 사용자는 작업을 완료 처리할 수 있어야 하며 완료 시각을 기록해야 한다.
- 사용자는 작업을 삭제할 수 있어야 한다.

### FR-07 주간 기록
- 최근 7일 기준 생성/완료 수, 완료율, 피로도 버킷(low/medium/high), 미완료-기한초과 작업 난이도 분포를 제공해야 한다.

## 8. 비기능 요구사항 (Non-functional Requirements)
- **신뢰성**: 외부 AI 실패 시에도 핵심 사용자 흐름(생성/추천/수정/완료)은 동작해야 한다.
- **성능**: 일반적인 단일 사용자 환경에서 페이지 로드/저장은 체감 지연 없이 동작해야 한다.
- **운영성**: 로그로 AI 소스/실패 유형을 확인 가능해야 한다.
- **배포성**: Azure App Service 배포를 지원해야 한다.

## 9. 성공 지표 (Success Metrics)
- 작업 생성 후 분석 결과 노출 성공률
- AI 실패 상황에서 fallback으로 흐름 지속 성공률
- 주간 완료율 개선 추세(개인 사용 단위)
- 수동 수정 이후 재사용되는 학습 프로필 누적 횟수(editCount)

## 10. 범위
### In Scope
- 단일 사용자 웹 환경의 작업/분석/추천/주간기록
- AI 분석 + fallback + 수동 보정 학습 루프
- 파일 기반 영속화(JSON)

### Out of Scope
- 멀티 사용자 인증/권한
- 데이터베이스 고도화/분산 아키텍처
- 외부 일정/협업 툴 통합

## 11. 릴리스 수용 기준 (Acceptance Criteria)
1. 제목 입력 후 작업 생성 시 분석 결과가 저장되고 `/analysis`로 이동한다.
2. 피로도 입력 후 홈/분석에서 추천 순위가 반영된다.
3. 작업 상세에서 분석 수정 저장 시 즉시 반영되고 학습 프로필이 업데이트된다.
4. 작업 완료 후 `/weekly-record`에서 주간 통계가 확인된다.
5. 외부 AI 호출 실패 상황에서도 작업 생성/추천이 가능하다.

## 12. 리스크 및 대응
- **외부 AI 의존성 리스크**: 네트워크/인증 오류 가능  
  → 휴리스틱 fallback 유지, 로그 기반 모니터링
- **초기 추정 부정확성**: 개인별 편차 큼  
  → 수동 수정 학습 루프로 개인화 보정
- **파일 저장 손상 가능성**: 비정상 종료 시 데이터 이슈  
  → 순차 쓰기(queue)와 기본 데이터 구조 보정 유지
